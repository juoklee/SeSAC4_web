<html>
    <head>
        <title> index </title>
        <style>
            .modal-bg{
                width: 100%;
                height: 100%;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                background-color: darkgray;
                z-index:0;
            }
            .modal-wrap {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                width: 300px;
                height: 300px;
                background-color: aliceblue;
                z-index:1;
            }
            .header{
                background-color: cadetblue;
                color: white;
            }
        </style>
        <script>
            function close() {
                var checkboxs = document.getElementsByName("close");
                if (checkboxs[0].checked) {
                    let date = new Date(Date.now());
                    date = date.toUTCString();
                    document.cookie = "key=value; expires=" + data;

                    let wrap = documne
                }     
            }
        </script>
    </head>
    <body>
        <div class="modal-bg"></div>
        <div class="modal-wrap">
            <div class="row header">안내</div>

            <label><input id="close" type="checkbox" name="close">오늘 하루 그만보기</label>
            <button type="button" onclick="close();">닫기</button>
        </div>
        
    </body>
</html>